import { useState } from 'react'

function App() {
  // const [count, setCount] = useState(0)
  const userID = 12;

  return (
    <div>
      <p>Hello this is my persional site {userID}</p>
      {/*anything written under { } in html tag is eviluation expression it means it takes only final output  */}
      
    </div>

    // userID -> this is invalid 
    
  )
}

export default App
